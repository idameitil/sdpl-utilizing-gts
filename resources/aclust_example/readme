sh /work3/idamei/bin/aclust_example/aclust1.sh [tree_directory] [fasta_filename]

# When all jobs are finished, run
sh /work3/idamei/bin/aclust_example/aclust2.sh [tree_directory] [fasta_filename]

# When job is finished, the tree file is in `[tree_directory]/my.tree`
